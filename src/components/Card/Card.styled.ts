import styled from "@emotion/styled"

export const  CardWrapper  = styled.div`
    width: 300px;
    height: 440px;
    background: #FCFCFC;
    box-shadow: 10px 6px 12px rgba(0, 0, 0, 0.03);
    border-radius: 4px;
    padding: 34px 36px;
    text-align: center;
`